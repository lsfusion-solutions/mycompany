MODULE SalesOrderGeo;

REQUIRE SalesOrder, PartnerGeo;

NAMESPACE Sale;

latitude 'Широта (покупатель)' (Order o) = latitude(customer(o));
longitude 'Долгота (покупатель)' (Order o) = longitude(customer(o));

EXTEND FORM orders
    PROPERTIES(o) READONLY latitude, longitude
;