MODULE SalesOrderPack;

REQUIRE ItemSalesPack, BarCodePack,
        SalesOrder, SalesOrderSearch;

NAMESPACE Sales;

@defineDocPack(order, sales, item, quantity, o);
DESIGN orderType { otherInformation { MOVE PROPERTY(pack(o)); } }

@defineDocSearchPack(order, sales, o);

@defineBarCodeSearchPack(order, l, quantity);
