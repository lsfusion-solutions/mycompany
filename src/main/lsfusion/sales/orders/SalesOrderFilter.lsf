MODULE SalesOrderFilter;

REQUIRE SalesOrder;

NAMESPACE Sales;

@defineDateFilterForm(orders, o, );

// right pane
@defineDocFilter(order, OrderType, type, o, 'Type');
@defineDocFilter(order, Location, location, o, 'Location');
@defineDocFilter(order, Partner, customer, o, 'Customer');