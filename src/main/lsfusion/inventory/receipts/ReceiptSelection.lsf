MODULE ReceiptSelection;

REQUIRE Receipt;

NAMESPACE Inventory;

@defineSelection(Receipt);

@defineSelectionForm(receipts, Receipt, r);

@defineSelectionCopyDateTime(receipts, Receipt, scheduledDateTime, 'начала');

@defineSelectionDelete(receipts, Receipt);