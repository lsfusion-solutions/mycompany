MODULE ShipmentReceiptCost;

REQUIRE ReceiptCost, ShipmentReceipt;

NAMESPACE Inventory;

cost(ReceiptLine l) += WHEN referenceShipment(l) THEN NUMERIC[14,2](done(referenceShipment(l)) * cost(product(l)));
