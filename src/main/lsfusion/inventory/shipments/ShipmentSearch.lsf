MODULE ShipmentSearch;

REQUIRE ShipmentDone, Doc;

NAMESPACE Inventory;

@defineDocSearch(shipment, product, initialDemand,s);

DESIGN shipment {
    pane2 {
        type = TABBED;
        NEW pane33 {
            caption = 'Подбор';
            type = SPLITH;
            fill = 1;
            MOVE BOX(TREE categories);
            MOVE BOX(itm) { 
                fill = 2; 
            }
        }        
    }
}
