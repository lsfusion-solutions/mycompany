MODULE InvoicePack;

REQUIRE ItemSalesPack, BarCodePack,
        Invoice;

NAMESPACE Invoicing;

@defineDocPack(invoice, sales, item, quantity, i);
DESIGN invoiceType { otherInformation { MOVE PROPERTY(pack(o)); } }

@defineDocSearchPack(invoice, sales, i);

@defineBarCodeSearchPack(invoice, l, quantity);