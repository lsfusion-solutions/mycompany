MODULE InvoiceFilter;

REQUIRE Invoice;

NAMESPACE Invoicing;

@defineDateFilterForm(invoices, i);

// right pane
@defineDocFilter(invoice, InvoiceType, type, i, 'Тип');
@defineDocFilter(invoice, Partner, customer, i, 'Покупатель');
