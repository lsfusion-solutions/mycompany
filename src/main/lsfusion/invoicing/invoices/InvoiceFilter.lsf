MODULE InvoiceFilter;

REQUIRE Invoice;

NAMESPACE Invoicing;

@defineDateFilterForm(invoices, i, );

// right pane
@defineDocFilter(invoice, InvoiceType, type, i, 'Type');
@defineDocFilter(invoice, Partner, customer, i, 'Customer');
@defineDocFilterPropertyForm(invoice, customer, 'Customer code', id);
