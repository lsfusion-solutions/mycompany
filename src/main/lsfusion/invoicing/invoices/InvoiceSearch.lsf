MODULE InvoiceSearch;

REQUIRE Invoicing, Invoice, Doc;

NAMESPACE Invoicing;

@defineDocSearch(invoice, item, quantity, i);

EXTEND FORM invoice
    FILTERGROUP active
        FILTER 'Активный' active(itm) 'F10' DEFAULT 
;
