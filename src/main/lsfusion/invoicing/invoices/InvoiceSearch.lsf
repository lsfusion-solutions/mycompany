MODULE InvoiceSearch;

REQUIRE Invoicing, InvoiceDone, InventoryReport, Doc;

NAMESPACE Invoicing;

@defineDocSearch(invoice, item, quantity, i);

EXTEND FORM invoices
    PROPERTIES(i) copyInvoice TOOLBAR 
;

EXTEND FORM invoice
    FILTERGROUP active
        FILTER 'Активный' active(itm) 'F10' DEFAULT 
;
