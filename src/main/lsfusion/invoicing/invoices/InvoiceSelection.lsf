MODULE InvoiceSelection;

REQUIRE Invoice, Selection;

NAMESPACE Invoicing;

@defineSelection(Invoice);

@defineSelectionForm(invoices, Invoice, i);

@defineSelectionCopyDateTime(invoices, Invoice, dateTime, );

@defineSelectionDelete(invoices, Invoice);