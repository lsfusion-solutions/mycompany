MODULE BillPayment;

REQUIRE BillOutstandingCredit, Payment;

NAMESPACE Invoicing;

EXTEND CLASS Payment : BillOutstandingCredit;
amount(Payment p) += amount(p);
account(Payment p) += fromAccount(p);
date(Payment p) += date(p);
number(Payment p) += number(p);



