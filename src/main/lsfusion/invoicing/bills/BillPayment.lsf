MODULE BillPayment;

REQUIRE Invoice, Bill, Payment;

NAMESPACE Invoicing;

bill 'Счет' = DATA Bill (Payment);

paid 'Оплачено' (Bill b) = GROUP SUM amount(Payment p) IF bill(p) = b;

EXTEND FORM bills
    PROPERTIES(b) READONLY paid
;

EXTEND FORM bill
    PROPERTIES (bb) paid
;