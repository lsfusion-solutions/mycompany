MODULE BillSelection;

REQUIRE Bill;

NAMESPACE Invoicing;

@defineSelection(Bill);

@defineSelectionForm(bills, Bill, b);

@defineSelectionCopyDateTime(bills, Bill, dateTime, );

@defineSelectionDelete(bills, Bill);