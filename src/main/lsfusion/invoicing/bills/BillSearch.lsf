MODULE BillSearch;

REQUIRE Invoicing, BillDone, InventoryReport, Doc;

NAMESPACE Invoicing;

@defineDocSearch(bill, item, quantity, b);

EXTEND FORM bills
    PROPERTIES(b) copyBill TOOLBAR
;

EXTEND FORM bill
    FILTERGROUP active
        FILTER 'Активный' active(itm) 'F10' DEFAULT 
;
