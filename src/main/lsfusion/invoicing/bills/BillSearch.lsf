MODULE BillSearch;

REQUIRE Invoicing, Bill, Doc;

NAMESPACE Invoicing;

@defineDocSearch(bill, item, quantity, b, canBePurchased);

EXTEND FORM bill
    FILTERGROUP active
        FILTER 'Активный' active(itm) 'F10' DEFAULT 
;
