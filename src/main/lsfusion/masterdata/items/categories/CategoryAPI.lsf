MODULE CategoryAPI;

REQUIRE Category;

NAMESPACE MasterData;

// http://localhost:8080/exec/MasterData.getCategories

getCategories () {
    EXPORT JSON FROM
        id(Category i),
        name(i), 
        parent = idParent(i)
        WHERE id(i);
} @@api;