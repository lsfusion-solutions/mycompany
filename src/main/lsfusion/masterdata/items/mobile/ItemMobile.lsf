MODULE ItemMobile;

REQUIRE Item, BarCode;

NAMESPACE MasterData;

FORM dialogItemsMobile 'Select item'
    OBJECTS i = Item
    PROPERTIES(i) READONLY name, id, idBarCode
    ORDERS name(i) 
;

DESIGN dialogItemsMobile {
    OBJECTS {
        MOVE GRID(i);
        REMOVE BOX(i);
    }
}
