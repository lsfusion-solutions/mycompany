MODULE PurchaseReport;

REQUIRE Purchase;

NAMESPACE Purchase;

attribute(OrderLine l, ItemAttribute a) = value(attribute(item(l), a));

FORM reportPurchaseOrders 'Отчет по заказам'
    OBJECTS a = ItemAttribute

    OBJECTS report = (o = Order, l = OrderLine)
    PROPERTIES(o) READONLY status, date, seriesNumber, nameLocation, nameVendor, vendorReference, scheduledDate, nameIncoterm,
                            namePaymentterm, nameRepresentative, untaxedAmount, totalTax, totalAmount
    PROPERTIES(l) READONLY nameItem 
    PROPERTIES READONLY attribute(l, a) COLUMNS (a) HEADER name(a)
    PROPERTIES(l) READONLY description, quantity, codeUnitMeasure, unitPrice, scheduledDate, taxes, subtotal
    FILTERS order(l) = o
;

NAVIGATOR {
    purchase {
        NEW FOLDER reports 'Отчеты' {
            NEW reportPurchaseOrders;        
        }
    }
}
