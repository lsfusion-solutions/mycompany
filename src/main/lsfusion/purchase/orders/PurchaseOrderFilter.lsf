MODULE PurchaseOrderFilter;

REQUIRE PurchaseOrder;

NAMESPACE Purchase;

@defineDateFilterForm(orders, o);

// right pane
@defineDocFilter(order, OrderType, type, o, 'Тип');
@defineDocFilter(order, Location, location, o, 'Место хранения');
@defineDocFilter(order, Partner, vendor, o, 'Поставщик');