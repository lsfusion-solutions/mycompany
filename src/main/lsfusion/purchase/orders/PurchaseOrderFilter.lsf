MODULE PurchaseOrderFilter;

REQUIRE PurchaseOrder;

NAMESPACE Purchase;

@defineDateFilterForm(orders, o, , filters);

// right pane
@defineDocFilter(order, OrderType, type, o, 'Type');
@defineDocFilter(order, Location, location, o, 'Location');
@defineDocFilter(order, Partner, vendor, o, 'Vendor');