MODULE PurchasePricelistItem;

REQUIRE PurchasePricelist;

NAMESPACE Purchase;

EXTEND FORM item 
    OBJECTS prlp = PricelistLine 
    PROPERTIES(prlp) READONLY number, startDateTime, endDateTime, note, nameItemVendor, codeItemVendor, price
    FILTERS item(prlp) = i
;

DESIGN item {
    purchase {
        MOVE BOX(prlp);
    }
}
