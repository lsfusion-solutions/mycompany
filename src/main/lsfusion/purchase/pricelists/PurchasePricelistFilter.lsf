MODULE PurchasePricelistFilter;

REQUIRE PurchasePricelist;

NAMESPACE Purchase;

@defineDateFilterForm(pricelists, p, start);

// right pane
@defineDocFilter(pricelist, Partner, vendor, p, 'Vendor');
@defineDocFilterPropertyForm(pricelist, vendor, 'Vendor code', id);
