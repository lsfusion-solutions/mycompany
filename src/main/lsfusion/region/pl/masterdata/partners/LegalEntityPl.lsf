MODULE LegalEntityPl;

REQUIRE LegalEntity, PartnerPl;

NAMESPACE MasterData;

nip 'NIP' = DATA STRING[10] (LegalEntity);
legalEntityNip = GROUP MAX LegalEntity b BY nip(b);
nip (LegalEntity l) += nip(l);
