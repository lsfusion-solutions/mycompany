MODULE ProjectTimeEntryPayslipInitial;

REQUIRE ProjectTimeEntryPayslip;

NAMESPACE ProjectManagement;

loadDefaultData () + {
    NEW c = PayslipCategory {
        name(c) <- 'Time entry';
        editable(c) <- TRUE;

        timeEntryPayslipCategory() <- c;
    }
}
