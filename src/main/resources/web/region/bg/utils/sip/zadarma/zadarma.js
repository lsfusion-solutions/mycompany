function zadarmaLoadWebRTC (key, sip) {
    zadarmaWidgetFn(
        key,
        sip,
        'square', /*square|rounded*/
        'ru', /*ru, en, es, fr, de, pl, ua*/
        true,
        {left:'10px',bottom:'5px'}
    );
}