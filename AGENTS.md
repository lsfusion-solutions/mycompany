# AGENTS.md

Памятка для AI-агентов и инженеров, работающих в этом репозитории (lsFusion-решение MyCompany).

## Интеграция с IDE (IntelliJ IDEA)

Предполагается, что параллельно запущен экземпляр IntelliJ IDEA с открытым этим
проектом, в котором настроена **MCP-интеграция**, публикующая возможности IDE в
виде инструментов. Агент **обязан** использовать эти IDE-инструменты как основной
способ работы с lsFusion-кодом — поиск, правка и инспекции идут через них.

> **Интеграция опознаётся по набору предоставляемых инструментов** —
> `lsfusion_find_elements`, `replace_text_in_file`, `create_new_file`,
> `get_file_problems` — **а не по имени MCP-сервера.** Имя сервера / префикс
> пространства имён может отличаться от установки к установке; стабильны именно
> имена инструментов. Использовать тот подключённый MCP-сервер, который их
> предоставляет.

### Справочник по lsFusion

Для справки по языку и парадигме lsFusion использовать инструменты
`lsfusion_retrieve_docs` и `lsfusion_get_guidance`. Вызов `lsfusion_get_guidance`
к тому же **обязателен** перед работой с `.lsf` — см. раздел ниже.

## Конвенции репозитория

- Источник правды для lsFusion-кода: `src/main/lsfusion`. Файлы в `out/production`
  и `target/` — генерируемые/собранные, править нельзя.

### Обязательный вызов `lsfusion_get_guidance` перед работой с `.lsf`

Перед первой за сессию операцией с `.lsf`-файлом (поиск, чтение, анализ или
правка) агент **обязан** один раз вызвать `lsfusion_get_guidance` и далее
применять полученные правила. Без него даже просто корректно прочитать
lsFusion-код почти невозможно — тонкости (трёхзначная логика, семантика сессий,
конвенции именования) из самого кода не выводятся. Повторный вызов в той же
сессии не нужен.

Если инструмент помечен как deferred — сначала подгрузить его схему через
`ToolSearch`.

### Приоритетные инструменты

Для любой работы с `.lsf`-кодом предпочитать эти три инструмента генерическим
аналогам.

#### 1. `lsfusion_find_elements` — поиск и анализ lsFusion-кода

- **Предпочитать его `Grep` / `Glob`** для любого семантического поиска классов,
  свойств, действий, форм, модулей и метакода. Он понимает пространства имён,
  сигнатуры, раскрытие метакода и граф использований — текстовый поиск этого не
  умеет.
- `Grep` использовать только для не-lsFusion-файлов или поиска буквального текста.
- Замечание: читает in-memory индекс IDEA. См. раздел *Устаревание индекса*.

#### 2. `replace_text_in_file` — правка `.lsf`-файлов

Для изменения кода использовать `replace_text_in_file`, для новых файлов —
`create_new_file`.

- **Предпочитать их `Edit` / `Write`.** Этот инструмент пишет через собственный
  VFS IDEA, поэтому изменение сразу видно `lsfusion_find_elements` и остальным
  IDE-инструментам.
- Правки через `Edit` / `Write` внешние по отношению к IDEA — индекс от них
  отстаёт (см. *Устаревание индекса*).

#### 3. `get_file_problems` — проверка после каждой правки

После каждой правки вызывать `get_file_problems` на каждом изменённом файле.

- Запускает инспекции IntelliJ с полным **семантическим** анализом lsFusion —
  ловит синтаксические ошибки и неразрешённые ссылки (например,
  `Property or action '…' not found`), сообщает severity и строку/колонку.
- Всегда **актуален**: отражает внешние правки немедленно.
- Оговорка: разрешение символов между файлами идёт через индекс, поэтому сразу
  после добавления символа в *другом* файле ссылка на него может ненадолго ложно
  показываться как «not found».
- Оговорка: на **свёрнутом** метакоде (`@metaName(args);` без раскрытого тела)
  `get_file_problems` может выдавать ошибки на код, который метакод сгенерирует.
  Это ожидаемо — не разворачивать метакод ради «чистого» отчёта.

### Стандартный рабочий процесс

1. **Найти** — `lsfusion_find_elements`: локализовать целевые элементы и понять контекст.
2. **Править** — `replace_text_in_file` / `create_new_file` (не `Edit` / `Write`).
3. **Проверить** — `get_file_problems` на каждом изменённом файле; исправить все
   сообщённые ошибки.

### Устаревание индекса

`lsfusion_find_elements` читает in-memory индекс IDEA. Правки **вне** IDEA
(`Edit` / `Write` или любой не-IDE-инструмент) не отражаются, пока IDEA не обновит
VFS — задержка от секунд до десятков секунд. Правка через `replace_text_in_file`
снимает это полностью: индекс актуален в момент возврата вызова.

### Чего не делать

- Не использовать MCP-инструменты, публикуемые claude.ai (имена с префиксом
  `mcp__claude_ai_*`) — работать через IDE-интеграцию, описанную выше.
- Не использовать `Grep` / `Edit` / `Write` для `.lsf`-кода там, где применимы
  инструменты выше.

### Свёрнутый метакод в коммитах `.lsf`

В коммит должны попадать только **свёрнутые** обращения к метакоду — одна строка
вида `@metaName(arg1, arg2, ...);`. Развёрнутая форма (когда плагин lsFusion в
IDEA «расклеил» тело метакода в исходник) в репозиторий идти не должна: она
дублирует сгенерированный код, ломает дальнейшие правки шаблона и засоряет diff.

**Признак раскрытого метакода — фигурная скобка `{` сразу после `@metaName(args)`**
(вместо `;`). При обнаружении `@name(args){` — **не коммитить**, сообщить
пользователю и попросить выполнить в IDEA `Hide Meta for File`. Прямого
MCP-инструмента для сворачивания метакода нет. Никогда не разворачивать метакод
самостоятельно ради правок «пары строк внутри» — менять сам `META`-шаблон.

### Проверка результата правок `.lsf`: только запуск службы

В lsFusion успешная компиляция модуля не гарантирует корректность кода —
**критерий «задача решена» для .lsf — это успешный запуск службы** и появление
маркеров готовности в логе. Если запуск службы недоступен, явно сказать об этом
и указать точный непроверенный риск.

## Правила работы нескольких агентов

Несколько агентов могут работать в репозитории одновременно. Чтобы правки не
конфликтовали:

1. **Владение и область.** Перед правкой объявить, какие модули / файлы
   затрагиваются. Держать область узкой — один модуль или одна связная фича на
   агента. Не делать оппортунистических правок вне заявленной области.

2. **Избегание коллизий.** Перечитывать целевой файл непосредственно перед
   патчем. Если тот же файл / модуль может править другой агент — остановиться и
   сузить область либо скоординироваться через пользователя. Избегать массового
   переформатирования, переименований и перемещений файлов без явного запроса.

3. **Дисциплина патчей.** Делать минимальные локальные изменения. Сохранять
   несвязанные правки пользователя. Не редактировать сгенерированный вывод в
   `out/production`; источник правды — `src/main/lsfusion`.

4. **Верификация.** После правок проверять изменённую область lsFusion доступными
   проектными / IDE-средствами (`get_file_problems`, при необходимости запуск
   службы). Если проверка невозможна — сказать об этом и указать точный
   непроверенный риск.

## Порядок работы по умолчанию

Начиная работу в этом репозитории, придерживаться порядка:

1. осмотреть `src/main/lsfusion`;
2. сначала определить нужный модуль и классы;
3. искать элементы проекта структурным поиском (`lsfusion_find_elements`) с
   правильными фильтрами;
4. подтянуть подходящие how-to и документацию (`lsfusion_retrieve_docs`) для
   задействованных типов элементов;
5. править только исходники, не сгенерированный вывод;
6. перечитать целевой файл прямо перед применением патча, если активны несколько
   агентов.
